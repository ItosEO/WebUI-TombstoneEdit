{
  "name": "coloros-tombstone-editor-webui",
  "version": "1.0.0",
  "description": "ColorOS Tombstone Config Editor KernelSU WebUI",
  "license": "MIT",
  "scripts": {
    "dev": "parcel module/webroot/index.html --open --no-cache",
    "build": "parcel build module/webroot/index.html --dist-dir module/webroot --no-source-maps --public-url ./ --no-cache"
  },
  "dependencies": {
    "kernelsu": "^1.0.6"
  },
  "devDependencies": {
    "parcel": "^2.10.0"
  }
}