{
  "name": "coloros-tombstone-editor-webui",
  "version": "1.0.0",
  "description": "ColorOS Tombstone Config Editor KernelSU WebUI",
  "license": "MIT",
  "scripts": {
    "dev": "parcel src/index.html --open --no-cache",
    "build": "parcel build src/index.html --dist-dir module/webroot --no-source-maps --public-url ./ --no-cache"
  },
  "dependencies": {
    "kernelsu": "^1.0.6"
  },
  "devDependencies": {
    "parcel": "^2.10.0"
  },
  "alias": {
    "kernelsu": "./kernelsu-stub.js"
  }
}